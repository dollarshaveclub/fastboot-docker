sudo: required

language: node_js
node_js:
  - "6"

services:
  - docker

install:
  - cd server-fastboot-docker/ && yarn && cd ..
  - cd test/ && yarn && cd ..

script:
  - ./scripts/ci/test.sh

after_script:
  - docker stop fastboot-app
  - docker images -q | xargs docker rmi -f
